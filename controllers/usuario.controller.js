const { connection } = require('../config/database');const { hashPassword } = require('../utils/hash.utils');const GetAllUsuarios = (req, res) => {    const query = 'SELECT id_usuario, usuario_nombre, usuario_mail, administrador, activo_usuario, habilitado FROM usuario WHERE activo_usuario = 1';    connection.query(query, (error, results) => {        if (error) {            return res.status(500).json({ error: 'Error al obtener los usuarios' });        }        res.status(200).json(results);    }    );};const GetUsuarioById = (req, res) => {    const { id } = req.params;    const query = 'SELECT * FROM usuario WHERE id_usuario = ? AND activo_usuario = 1';    connection.query(query, [id], (error, results) => {        if (error) {            return res.status(500).json({ error: 'Error al obtener el usuario' });        }        if (results.length === 0) {            return res.status(404).json({ error: 'Usuario no encontrado' });        }        res.status(200).json(results[0]);    });}const CreateUsuario = (req, res) => {    const { usuario_nombre, usuario_mail, usuario_contrasena, administrador, habilitado } = req.body;    if (!usuario_nombre || !usuario_mail || !usuario_contrasena) {        return res.status(400).json({ error: 'usuario_nombre, usuario_mail y usuario_contrasena son requeridos' });    }    const isAdmin = administrador ? 1 : 0;    const isHabilitado = habilitado !== undefined && !habilitado ? 0 : 1;    hashPassword(usuario_contrasena).then((hashedPassword) => {        connection.getConnection((err, conn) => {            if (err) return res.status(500).json({ error: 'Error al obtener conexión' });            conn.beginTransaction((err) => {                if (err) {                    conn.release();                    console.error('Error al iniciar transacción:', err);                    return res.status(500).json({ error: 'Error al iniciar transacción' });                }                const qCheck = 'SELECT id_usuario FROM usuario WHERE usuario_mail = ? AND activo_usuario = 1';                conn.query(qCheck, [usuario_mail], (errCheck, resultsCheck) => {                    if (errCheck) {                        return conn.rollback(() => {                            conn.release();                            console.error('Error en query comprobación usuario:', errCheck);                            res.status(500).json({ error: 'Error al crear el usuario', details: errCheck.message });                        });                    }                    if (resultsCheck.length > 0) {                        return conn.rollback(() => {                            conn.release();                            return res.status(400).json({ error: 'El correo electrónico ya está registrado' });                        });                    }                    const qInsert = 'INSERT INTO usuario (usuario_nombre, usuario_mail, usuario_contrasena, administrador, habilitado) VALUES (?, ?, ?, ?, ?)';                    conn.query(qInsert, [usuario_nombre, usuario_mail, hashedPassword, isAdmin, isHabilitado], (errInsert, resultsInsert) => {                        if (errInsert) {                            return conn.rollback(() => {                                conn.release();                                console.error('Error al insertar usuario:', errInsert);                                res.status(500).json({ error: 'Error al crear el usuario', details: errInsert.message });                            });                        }                        conn.commit((errCommit) => {                            if (errCommit) {                                return conn.rollback(() => {                                    conn.release();                                    console.error('Error al confirmar transacción:', errCommit);                                    res.status(500).json({ error: 'Error al confirmar la creación' });                                });                            }                            conn.release();                            res.status(201).json({ id_usuario: resultsInsert.insertId, usuario_nombre, usuario_mail, administrador: !!isAdmin, habilitado: !!isHabilitado });                        });                    });                });            });        });    }).catch((hashErr) => {        console.error('Error al hashear contraseña:', hashErr);        res.status(500).json({ error: 'Error interno al procesar la contraseña' });    });};const UpdateUsuario = async (req, res) => {    try {        const { id } = req.params;        const { usuario_nombre, usuario_mail, usuario_contrasena, administrador, habilitado } = req.body;        const isAdmin = administrador ? 1 : 0;        const isHabilitado = habilitado !== undefined && !habilitado ? 0 : 1;        if (typeof usuario_contrasena !== 'undefined' && usuario_contrasena !== null && usuario_contrasena !== '') {            const hashed = await hashPassword(usuario_contrasena);            const query = 'UPDATE usuario SET usuario_nombre = ?, usuario_mail = ?, usuario_contrasena = ?, administrador = ?, habilitado = ? WHERE id_usuario = ?';            connection.query(query, [usuario_nombre, usuario_mail, hashed, isAdmin, isHabilitado, id], (error, results) => {                if (error) {                    console.error('UpdateUsuario DB error:', error);                    return res.status(500).json({ error: 'Error al actualizar el usuario en la base de datos', details: error.message });                }                if (results.affectedRows === 0) {                    return res.status(404).json({ error: 'Usuario no encontrado' });                }                return res.status(200).json({ id_usuario: id, usuario_nombre, usuario_mail, administrador: !!isAdmin, habilitado: !!isHabilitado });            });        } else {            const query = 'UPDATE usuario SET usuario_nombre = ?, usuario_mail = ?, administrador = ?, habilitado = ? WHERE id_usuario = ?';            connection.query(query, [usuario_nombre, usuario_mail, isAdmin, isHabilitado, id], (error, results) => {                if (error) {                    console.error('UpdateUsuario DB error:', error);                    return res.status(500).json({ error: 'Error al actualizar el usuario en la base de datos', details: error.message });                }                if (results.affectedRows === 0) {                    return res.status(404).json({ error: 'Usuario no encontrado' });                }                return res.status(200).json({ id_usuario: id, usuario_nombre, usuario_mail, administrador: !!isAdmin, habilitado: !!isHabilitado });            });        }    } catch (err) {        console.error('UpdateUsuario Catch error:', err);        res.status(500).json({ error: 'Error interno del servidor al actualizar usuario', details: err.message });    }}const deleteUsuario = (req, res) => {    const { id } = req.params;    const query = 'UPDATE usuario SET activo_usuario = 0 WHERE id_usuario = ?';    connection.query(query, [id], (error, results) => {        if (error) {            return res.status(500).json({ error: 'Error al eliminar el usuario' });        }        if (results.affectedRows === 0) {            return res.status(404).json({ error: 'Usuario no encontrado' });        }        res.status(204).send();    });}module.exports = {    GetAllUsuarios,    GetUsuarioById,    CreateUsuario,    UpdateUsuario,    deleteUsuario};